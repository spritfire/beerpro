<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorMenuBackground">@color/colorMenuBackground</item>
        <item name="colorMenuText">@color/colorMenuText</item>
        <item name="colorListEntry">@color/colorListEntry</item>
        <item name="colorSearchTerm">@color/colorSearchTerm</item>
        <item name="colorRating">@color/colorRating</item>
        <item name="colorPrimaryLight">@color/colorPrimaryLight</item>

        <item name="android:windowBackground">@color/windowBackgroundColor</item>

        <item name="styleMyToolbar">@style/ToolBarStyle</item>

        <!-- enable window content transitions -->
        <item name="android:windowActivityTransitions">true</item>

        <!-- specify shared element transitions -->
        <item name="android:windowSharedElementEnterTransition">@transition/change_view_transform
        </item>
        <item name="android:windowSharedElementExitTransition">@transition/change_view_transform
        </item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>


    <style name="DetailsPageTheme" parent="AppTheme">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="CollapsingToolbarLayoutExpandedTextStyle" parent="AppTheme">
        <item name="android:textColor">@android:color/transparent</item>
        <item name="android:textSize">0sp</item>
    </style>

    <style name="ToolBarStyle" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@color/colorAccent</item>
        <item name="android:textColorSecondary">@color/colorAccent</item>
        <item name="actionMenuTextColor">@color/colorAccent</item>
        <item name="titleMarginStart">@dimen/title_margin_start</item>
    </style>

    <style name="LoginScreenTheme" parent="AppTheme">
        <item name="android:windowBackground">@color/colorPrimary</item>
    </style>

    <style name="FirebaseUI.AuthMethodPicker.Logo">
        <item name="android:layout_width">160dp</item>
        <item name="android:layout_height">160dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:padding">0dp</item>
    </style>

    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/colorMenuText</item>
        <item name="colorControlActivated">@color/colorPrimary</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <dimen name="title_margin_start">24dp</dimen>

    <style name="AppTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/colorAccent</item>
        <item name="tabMaxWidth">256dp</item>
        <item name="tabIndicatorGravity">bottom</item>
        <item name="tabIndicatorHeight">4dp</item>
        <item name="tabPaddingStart">6dp</item>
        <item name="tabPaddingEnd">6dp</item>
        <!--<item name="android:layout_marginLeft">64dp</item>
        <item name="android:layout_marginRight">64dp</item>-->
    </style>

    <style name="FridgeListEntry" parent="AppTheme">
        <item name="android:padding">10dp</item>
        <item name="android:textSize">15sp</item>
    </style>


    <!-- Dark Theme -->
    <style name="AppTheme.Dark" parent="Theme.AppCompat">
        <item name="colorPrimary">@color/darkColorPrimary</item>
        <item name="colorPrimaryDark">@color/darkColorPrimaryDark</item>
        <item name="colorAccent">@color/darkColorAccent</item>
        <item name="colorMenuBackground">@color/darkColorMenuBackground</item>
        <item name="colorMenuText">@color/darkColorMenuText</item>
        <item name="colorListEntry">@color/darkColorListEntry</item>
        <item name="colorSearchTerm">@color/darkColorSearchTerm</item>
        <item name="colorRating">@color/darkColorRating</item>
        <item name="colorPrimaryLight">@color/darkColorPrimaryLight</item>

        <item name="android:windowBackground">@color/darkWindowBackgroundColor</item>

        <item name="styleMyToolbar">@style/ToolBarStyleDark</item>

        <!-- enable window content transitions -->
        <item name="android:windowActivityTransitions">true</item>

        <!-- specify shared element transitions -->
        <item name="android:windowSharedElementEnterTransition">@transition/change_view_transform
        </item>
        <item name="android:windowSharedElementExitTransition">@transition/change_view_transform
        </item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ToolBarStyleDark" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@color/darkColorAccent</item>
        <item name="android:textColorSecondary">@color/darkColorAccent</item>
        <item name="actionMenuTextColor">@color/darkColorAccent</item>
        <item name="titleMarginStart">@dimen/title_margin_start</item>
    </style>

    <style name="LoginScreenThemeDark" parent="AppTheme.Dark">
        <item name="android:windowBackground">@color/darkColorPrimary</item>
    </style>

    <style name="RatingBar.Dark" parent="AppTheme.Dark">
        <item name="colorControlNormal">@color/colorMenuText</item>
        <item name="colorControlActivated">@color/darkColorPrimary</item>
    </style>

    <style name="AppTheme.Dark.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.Dark.PopupOverlay" parent="ThemeOverlay.AppCompat" />

    <style name="AppTabLayout.Dark" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/darkColorAccent</item>
        <item name="tabMaxWidth">256dp</item>
        <item name="tabIndicatorGravity">bottom</item>
        <item name="tabIndicatorHeight">4dp</item>
        <item name="tabPaddingStart">6dp</item>
        <item name="tabPaddingEnd">6dp</item>
        <!--<item name="android:layout_marginLeft">64dp</item>
        <item name="android:layout_marginRight">64dp</item>-->
    </style>

    <style name="FridgeListEntryDark" parent="AppTheme.Dark">
        <item name="android:padding">10dp</item>
        <item name="android:textSize">15sp</item>
    </style>
</resources>
